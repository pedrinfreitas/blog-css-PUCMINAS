export interface IMedia {
  id: number;
  guid: {
    rendered: string;
  };
  slug: string;
  title: {
    rendered: string;
  };
  description: {
    rendered: string;
  };
  media_type: string;
  mime_type: string;
  post: 2425;
  source_url: string;
}
